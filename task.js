/**
 * 1. Array Length //
 * 2. Array Index() // ex: numbers[0] or array[1]
 * 3. Array push() pop()
 * -------- Push() -> insert in end : Pop() -> Remove form end---------
 *
 * 4. Array unshift() shift()
 * -------- unshift() -> insert in start : shift() -> remove from start------------
 *
 * ----- Special Methods---------
 * 5.Array Includes()
 * 6. Array indexOf() // By elements
 * 7. Array.isArray()
 * 8. join(+), Add array components by +
 * 9..concat(), Ex: array1.concat(array2);
 * 10. slice(2, 4), ex: numbers.slice()
 * 11. splice()
 *
 */
